encode
======

In computing, a character encoding is used to represent a repertoire
of characters by some kind of an encoding system.

The encode package on :github-tree:`Github<src/encode>`.

.. toctree::
   :glob:   
   :titlesonly:

   encode/*
