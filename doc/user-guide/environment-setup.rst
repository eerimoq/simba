Environment setup
=================

NOTE: This is only valid is you use the `Simba build system`.

The first step is always to setup the `Simba` environment. It's a
simple matter of sourcing a setup-script in the simba root folder.

.. code-block:: text

   $ cd simba/simba
   $ source setup.sh
